//a function that takes a variable number of parameters
function func1(thetext, repeattimes) {
  for (let i = 0; i < repeattimes; i++) {
    console.log(thetext);
  }
}

func1("i eat kids(as food)", 50);